// import axios from 'axios';

// const BASE_URL = '';

// const instance = axios.create({
//   baseURL: BASE_URL,
// });

export async function postFeedback(req) {
  // const { data } = await instance.post(`/feedback`, req);
  const data = { name: 'data' };
  console.log(data);
  return data;
}
